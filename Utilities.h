#pragma once
#include <vector>
#include <string>
#include <sstream>

class Utilities
{
public:
	static std::vector<std::string> split_string(const std::string& str, const std::string& delimiter);
};

